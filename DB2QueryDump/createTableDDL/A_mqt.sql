CREATE TABLE "ANALYZER_GOSALES_HR"."A_DEPARTMENTDATA"
     (DEPTNO     SMALLINT      NOT NULL,
      DEPTNAME   VARCHAR(36)   NOT NULL,
      MGRNO      CHAR(6),
      ADMRDEPT   SMALLINT      NOT NULL,
      LOCATION   CHAR(30));

CREATE TABLE "ANALYZER_GOSALES_HR"."A_DEPT_DETAILS" (DEPT_NO,DEPT_NAME,LOCATION) AS
  (SELECT DEPTNO,DEPTNAME,LOCATION
     FROM "ANALYZER_GOSALES_HR"."A_DEPARTMENTDATA"
 )
     DATA INITIALLY DEFERRED
     REFRESH DEFERRED
     MAINTAINED BY SYSTEM
     ENABLE QUERY OPTIMIZATION;